var React = require('react');

module.exports = Tweet = React.createClass({
  // Render the component
  render: function(){

    return (
      <div className="tweet">
          tweet item
      </div>
    )
  }
});
